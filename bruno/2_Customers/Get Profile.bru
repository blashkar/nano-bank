meta {
  name: Get Profile
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/customers/profile?customer_id={{customerId}}
  auth: inherit
}

params:query {
  customer_id: {{customerId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
