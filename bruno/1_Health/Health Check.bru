meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 200
  res.body.status: eq healthy
  res.body.services.database: eq healthy
}
