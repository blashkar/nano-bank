meta {
  name: Capture Authorization
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/cards/capture
  body: json
  auth: bearer
}

auth:bearer {
  token: {{serviceToken}}
}

body:json {
  {
    "auth_id": "{{authId}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
