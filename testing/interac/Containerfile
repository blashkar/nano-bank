FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY interac_simulator.py .

# All tunables come from env (see interac_simulator.py docstring).
ENTRYPOINT ["python", "interac_simulator.py"]
