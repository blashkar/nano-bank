FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY generate_customers.py .

# All tunables come from env (see generate_customers.py docstring).
ENTRYPOINT ["python", "generate_customers.py"]
