FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY visa_simulator.py .

# All tunables come from env (see visa_simulator.py docstring).
ENTRYPOINT ["python", "visa_simulator.py"]
